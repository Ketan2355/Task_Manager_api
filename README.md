
# Task Manager API

**Task Manager API** is a production-style RESTful backend built with **Python** and **Django** (Django REST Framework).  
It provides secure task management endpoints (CRUD) with JWT authentication, user-specific access control, and a modular architecture ready for deployment.

---

## Tech Stack
- Python 3.11+
- Django 5.x
- Django REST Framework (DRF)
- djangorestframework-simplejwt (JWT authentication)
- SQLite (development) — easily switchable to PostgreSQL
- python-dotenv for environment variables
- HTTPie / Postman for API testing

---



